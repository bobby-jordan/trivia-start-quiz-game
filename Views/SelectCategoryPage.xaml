<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TriviaStarQuizGame.Views.SelectCategoryPage"
             Title="Select a Category">
    <StackLayout Padding="20" Spacing="15">
        <Label Text="Select a Quiz Category:" FontSize="18" HorizontalOptions="Center" />

        <!-- BindableLayout for dynamic category buttons -->
        <StackLayout BindableLayout.ItemsSource="{Binding Categories}" Spacing="10">
            <BindableLayout.ItemTemplate>
                <DataTemplate>
                    <Button Text="{Binding .}" 
                            Command="{Binding BindingContext.SelectCategoryCommand, Source={RelativeSource AncestorType={x:Type ContentPage}}}" 
                            CommandParameter="{Binding .}" />
                </DataTemplate>
            </BindableLayout.ItemTemplate>
        </StackLayout>
    </StackLayout>
</ContentPage>
